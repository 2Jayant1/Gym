# ─── FitFlex Gym Management — Environment Variables ────────────
# Copy this file: cp .env.example Backend/.env
# Then fill in your values.

# ─── Server ──────────────────────────────────────────────────
PORT=5001
NODE_ENV=development                           # development | production | test

# ─── MongoDB ─────────────────────────────────────────────────
MONGODB_URI=mongodb://localhost:27017/gym_management

# ─── Authentication ──────────────────────────────────────────
AUTH_SECRET=change-me-to-a-long-random-string  # min 8 chars, use `openssl rand -base64 32`
JWT_EXPIRES_IN=2h                              # access token lifetime
JWT_REFRESH_EXPIRES_IN=7d                      # refresh token lifetime

# ─── CORS ────────────────────────────────────────────────────
CORS_ORIGIN=http://localhost:3000              # comma-separated for multiple origins (set to your real domain in prod)

# ─── Rate Limiting ───────────────────────────────────────────
RATE_LIMIT_WINDOW_MS=900000                    # 15 minutes
RATE_LIMIT_MAX=300                             # requests per window

# ─── Request Timeout ─────────────────────────────────────────
REQUEST_TIMEOUT_MS=30000                       # 30 seconds

# ─── Redis (optional — caching is graceful-degrading) ────────
# REDIS_URL=redis://localhost:6379

# ─── Logging ─────────────────────────────────────────────────
LOG_LEVEL=debug                                # error | warn | info | http | debug

# ─── Monitoring (optional) ───────────────────────────────────
# SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

# ─── Feature Flags (comma-separated) ─────────────────────────
# FEATURE_FLAGS=REDIS_CACHE,ADVANCED_ANALYTICS

# ─── ML / AI Service (FastAPI) ───────────────────────────────
# Used by the ML container and also by local dev.
# In production, keep DISABLE_AUTH=false.
DISABLE_AUTH=false

# ─── Ollama (optional local LLM runtime) ─────────────────────
# For Docker: set OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2:3b
